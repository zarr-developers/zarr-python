`pyproject.toml` no longer tells uv to resolve `zarr` from the zarr-python repository, so `just test` works from an unpacked sdist, against `zarr` from PyPI. Inside the repository, the justfile recipes add the in-repo `zarr`.

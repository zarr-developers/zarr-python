Define an explicit source token contract: hash supported plain NumPy arrays field by field without structured padding, delegate source hooks and propagate their errors, and reject unsupported sources without converting or serializing them. Object fields, dtype metadata, and non-string field titles require a source hook. Remove optional-Dask and per-call UUID fallbacks. Document hashing costs, mutation limits, and `name=False` for Dask wrapping opaque sources.

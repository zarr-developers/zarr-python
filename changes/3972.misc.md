Run `mypy` from the `hatch` `dev` environment instead of `pre-commit`'s isolated venv. The `dev` group in `pyproject.toml` (resolved via `uv.lock`) is now the single source of truth for `mypy`'s dependency set, eliminating the duplicate dependency list previously maintained in `.pre-commit-config.yaml`.

ChunkGrid objects now track the array shape they are associated with. The RegularChunkGrid class has been enhanced to accept an optional `array_shape` parameter during initialization, and a new `with_array_shape()` method allows setting or updating the shape on existing ChunkGrid instances. This enables better encapsulation of shape information within chunk grid objects without breaking backward compatibility (array_shape is not serialized to disk).

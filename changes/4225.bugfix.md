Fix the `test_docstring_consistent_parameters` test, which silently passed due to a tuple-iteration bug and a typo in the parameter list. Align the `create_array` docstrings (`filters`, `compressors`, `chunks`, `shape`, `dtype`) across `core.array`, `api.synchronous`, `AsyncGroup`, and `Group` so the test now enforces consistency.
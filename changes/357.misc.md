The example scripts in `examples/` and the `zarr-indexing` and `zarr-http-server` packages now install zarr (and their own package) from the checkout they are in, so `uv run <example>` runs the code next to it rather than a release or the `main` branch.

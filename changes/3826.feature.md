Added a `subchunk_write_order` option to `ShardingCodec` to control the physical order of subchunks within a shard. Supported values are `morton`, `lexicographic`, `colexicographic`, and `unordered`. `unordered` makes no guarantee about subchunk layout. This setting affects only on-disk layout, not the data read back.

Zarr format 3 group metadata now accepts extra fields that are objects with `"must_understand": false`, as the specification requires, and keeps them when the metadata is written back. Other extra fields raise `MetadataValidationError` instead of a `TypeError`.

Removed Zarr's `async.concurrency` limit and the internal `concurrent_map` helper.
Zarr no longer supplies a semaphore-based limit for store operations; configure
concurrency through the backend when it supports this, or provide a store or
adapter that implements the required limit. Existing custom stores that relied
on Zarr's limit may now receive more concurrent operations.

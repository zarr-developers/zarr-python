Validate codec chains against evolving chunk specs, including inner sharding chains and the actual fill value. Preserve factored grid geometry for default metadata resolvers, cast/scale codecs, and transpose so common chains validate per-axis sizes without enumerating chunk combinations. Stream arbitrary shape-changing chains without materializing the cross product.

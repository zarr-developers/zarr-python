Sharding `index_codecs` containing a variable-size codec, such as a compressor, are now rejected when the `ShardingCodec` is built, with a clear error. Before, writing failed later with an unexplained `NotImplementedError`.

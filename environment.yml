channels:
  - conda-forge
  - defaults
dependencies:
  - wheel
  - numcodecs >= 0.6.4
  - numpy >= 1.7
  - pip
  - werkzeug<2.2.0
  - pip:
    - asciitree
    - fasteners
    - pytest
    - pytest-timeout
    - setuptools_scm

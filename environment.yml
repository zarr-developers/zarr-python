channels:
  - conda-forge
  - defaults
dependencies:
  - wheel
  - numcodecs >= 0.6.4
  - numpy >= 1.7
  - pip
  - pip:
    - asciitree
    - fasteners == 0.17.1
    - pytest
    - pytest-timeout
    - setuptools_scm

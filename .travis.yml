language: python

branches:
  only:
    - master

sudo: false

addons:
  apt:
    packages:
    - libdb-dev

python:
  - 2.7
  - 3.5
  - 3.6
  - 3.7

install:
  - pip install -U pip setuptools wheel tox-travis coveralls

script:
  - tox

after_success:
  - coveralls
